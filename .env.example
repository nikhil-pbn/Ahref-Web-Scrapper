# Ahrefs API v3 token (Bearer). Generate it in Ahrefs: Account settings -> API.
# Requires a paid Ahrefs API subscription — calls consume API units.
# Copy this file to .env.local and fill in your token.
AHREFS_API_TOKEN=
